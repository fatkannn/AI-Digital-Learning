# M16.2 — Targeted Verification

Scope: exactly two questions carried over from M16.1 (§ GEMINI-CURRICULUM-ENHANCEMENT-MATRIX.md).
No other Gemini claim, resource, or curriculum day was touched. This is a verification-only
session — no file under `APPLICATION/` or `QA/` was edited.

---

## Day 23 — Microsoft Learn

| Field | Finding |
|---|---|
| Original URL (as recorded by Gemini, `EDTECH-001`) | `https://learn.microsoft.com/en-us/training/educator-center/` |
| Redirect chain | None — resolves directly, no redirect |
| Canonical URL | `https://learn.microsoft.com/en-us/training/educator-center/` (the page's own `canonicalUrl` metadata confirms this is already the canonical address) |
| Page title | "Educator Center Overview - Microsoft Learn Educator Center \| Microsoft Learn" |
| Active/inactive | **Active.** Page metadata shows `updated_at: 2026-07-20`, current |
| Official source | Yes — `learn.microsoft.com`, sourced from Microsoft's own `MicrosoftDocs/LearnShared` GitHub repo per the page's own build metadata |
| Actual content | This is **not** a focused "AI in education" page. It is the general **Educator Center hub** — a broad landing page covering six unrelated training topics (AI for education, classroom cybersecurity, Learning Accelerators, STEM/coding/esports, accessibility, academic research skilling), seven Microsoft product guides (Copilot, Teams, Minecraft Education, Microsoft 365, Search Coach, Immersive Reader, Reading Progress), several educator community programs, and instructor-material links (Hacking STEM, FarmBeats, Racing with Rajah). AI is one of ~13 top-level sections, not the page's subject |
| Relevance to Day 23 topic (AI in instructional design) | **Weak, at the root URL as recorded.** A single link on the page — "AI for education" (`/en-us/training/educator-center/topics/ai-for-education`) — is on-topic; the page itself is a general educator portal, not AI-focused |
| Relevance to Day 23's objective ("Use AI for first drafts while keeping a human-owned source of truth") | **Not directly supported.** The page is a navigation hub to Microsoft product training (chiefly Copilot for education), not content addressing grounding, human-owned source of truth, or assessment-integrity redesign — the specific concepts Day 23 teaches. It is closer to vendor product marketing than to the instructional-design guidance the day's objective targets |
| Difference from existing UNESCO resources | Different in kind, not depth: the UNESCO resources (already in Day 23, both `verified: true`) are vendor-neutral policy/guidance documents specifically on generative AI in education. This Microsoft page is a vendor-specific (Microsoft) training-and-product portal covering AI as one topic among many. It would not add a new *angle* on the day's actual objective so much as a different *category* of source (product hub vs. policy guidance) |
| Title accuracy | **Inaccurate as recorded.** Gemini's research (per M16.1's audit) recorded this as "Microsoft Learn — AI in Education." The page's real title and scope is "Educator Center Overview" — a general hub, not an AI-specific resource. This is a material mismatch between the claimed resource and what the URL actually contains |
| Suitable as optional further reading | Only the specific "AI for education" sub-page would fit that description; the recorded root URL is too broad and too product-promotional to serve as focused further reading for this day's objective |
| Meaningful reason to add (as recorded) | **No** — the exact URL Gemini recorded does not match its own claimed title or focus, is largely product marketing for Copilot/Teams/Minecraft/M365 rather than instructional-design guidance, and does not clearly support the day's stated learning objective better than what is already there |

**Recommendation: DO NOT ADD** (the resource as literally recorded by Gemini). The URL is real, active, and official, but it is not what Gemini's own research claimed it to be — a mismatch discovered only by opening it, which is exactly the kind of check this project's resource-verification process (M8–M15) exists to catch. Note for the record, not a proposal for this session: the page does contain a more specific sub-link, "AI for education," which was *not* independently verified here (out of scope — no additional-resource searching was permitted this session) and would need its own verification pass before any future consideration.

---

## Day 16 — Hugging Face

| Field | Finding |
|---|---|
| Current verified URL | `https://huggingface.co/learn` |
| Current URL result | Resolves. Page titled "Learn." Lists 12 course/learning-path offerings, with the **LLM Course listed first**: "This course will teach you about large language models using libraries from the HF ecosystem." Also lists Agents, Deep RL, Computer Vision, Audio, Diffusion, ML for Games/3D, and others |
| Gemini-suggested specific URL | `https://huggingface.co/learn/llm-course/` |
| Specific URL result | Resolves to real content — an actual, active LLM/NLP course (12 chapters across 4 sections: Transformers fundamentals, datasets/tokenizers/classic NLP tasks, model demos, advanced LLM topics including fine-tuning and reasoning models; free, ~6–8 hours/chapter, Apache 2.0 licensed, 20+ languages) |
| Redirect/canonical behavior | The specific URL does **not** land on a page whose own address is `https://huggingface.co/learn/llm-course/` — the content resolved reports its own location as `https://huggingface.co/course`, a **different path entirely** from the one Gemini recorded. A follow-up check for the exact HTTP redirect mechanics (status code, `Location` header) could not be confirmed — the fetch tool used here reports rendered content, not raw HTTP headers — but the practical result is consistent: the Gemini-recorded path is not itself a stable, self-identifying canonical address; it leads to content whose actual home is a separate, shorter path |
| Content relevance | Both the root `/learn` and the specific path lead to genuinely relevant, real LLM-course content — no dead link on either side |
| Navigation usefulness | The specific path would save a learner one click (skipping the 12-course selection page and landing directly on the LLM course) — a real but marginal convenience, not a difference in what content is reachable, since the LLM Course is already the first, prominently labeled item on the root page |
| Stability/verification consideration | The root URL (`/learn`) is a stable top-level hub already confirmed `verified: true` and unlikely to be restructured. The specific URL's own resolved location differs from the address Gemini recorded, which is itself a small instability signal — the exact path is not confirmed as Hugging Face's own canonical address for this course, only as *a* working entry point into it |

**Recommendation: KEEP CURRENT URL.** Per the default rule stated for this task — verified-and-broad beats more-precise-and-unverified unless the specific path is clearly active, canonical, relevant, *and* materially better — three of those four conditions hold (active, relevant, arguably better) but **canonical does not**: the specific URL's own resolved location is a different address than the one that would be recorded, meaning "updating to it" would mean recording a URL that isn't even the page's own home address. The improvement on offer (one fewer click) does not clear the bar of "materially better" against that instability. The currently verified `https://huggingface.co/learn` remains the correct stored URL.

---

## Owner Recommendation

- **Day 23 (Microsoft Learn / `EDTECH-001`): DO NOT ADD.** The recorded URL does not match Gemini's own claimed title or focus — it is a broad, largely product-promotional educator portal, not an "AI in Education" resource. If Microsoft Learn content is wanted for Day 23 in the future, the specific "AI for education" sub-page it links to would need its own independent verification pass — not proposed or scheduled here.
- **Day 16 (Hugging Face LLM course precision): KEEP CURRENT URL.** The specific path Gemini suggested works and is relevant, but its own resolved address differs from the one recorded, making it a worse canonical citation than the already-verified root — not a case where the evidence clears the bar for changing a verified URL.

## Implementation Status

**NO CURRICULUM CHANGES MADE.** `APPLICATION/ai-digital-learning-dashboard.jsx` was not opened for editing this session — read-only web verification only. No `verified` flag, URL, title, resource, objective, quiz question, or progression-logic line was touched. `EDTECH-001` was not added. The Hugging Face URL was not changed.
