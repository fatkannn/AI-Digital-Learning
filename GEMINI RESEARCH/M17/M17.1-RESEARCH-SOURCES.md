# M17.1 — Research Sources: Days 1–4

Every external source actually opened this session via WebFetch (or corroborated by an opened
WebSearch result, as noted). No search-result snippet was treated as evidence on its own without
opening the underlying page. Days 2–4 required no external research — the pedagogical audit found
no gap worth researching, and manufacturing sources for non-findings would violate the "research
only what is needed" instruction.

| # | Source | Organization | URL | Type | Relevant claim | Affected day | Credibility |
|---|---|---|---|---|---|---|---|
| 1 | "Copilot+ PCs developer guide" | Microsoft (Windows AI docs) | `https://learn.microsoft.com/en-us/windows/ai/npu-devices/` | Official vendor documentation | NPU is a dedicated AI-inference chip (40+ TOPS for Copilot+ certification), distinct from CPU/GPU, more power-efficient for AI workloads; Windows Task Manager now shows NPU usage alongside CPU/GPU/Memory/Disk; NPU hardware exists across Qualcomm, Intel, and AMD silicon, not one vendor | Day 1 | **A — Official/primary.** Page metadata confirms `updated_at: 2026-07-15`, sourced from Microsoft's own `MicrosoftDocs/windows-ai-docs-pr` repository |
| 2 | "All about neural processing units (NPUs)" | Microsoft Support | `https://support.microsoft.com/windows/all-about-neural-processing-units-npus-e77a5637-7705-4915-96c8-0c6a975f9db4` | Official vendor support documentation | Corroborates source 1: NPUs use less power and are more efficient at AI tasks than a CPU or GPU, and free up the CPU/GPU for other work | Day 1 | **A — Official/primary** |

## Attempted but not used as evidence

- `https://learn.microsoft.com/en-us/windows/ai/npu-overview` — guessed URL pattern, returned
  **HTTP 404**. Not used for anything; the correct official URL (source 1 above) was located via a
  proper `WebSearch` query rather than by guessing a second time, consistent with this project's
  standing rule never to construct or guess a resource URL.
- `https://developer.apple.com/machine-learning/core-ml/` — opened, but the fetch returned only
  the page title with no usable body content, so **no claim from this page is used as evidence**.
  The audit document explicitly flags the Apple Neural Engine parallel as general knowledge, not
  independently source-confirmed this session, rather than silently treating it as verified.

## Sources considered unnecessary

Days 2, 3, and 4 were pedagogically audited in full but yielded no genuine gap — see
`M17.1-DAYS-01-04-AUDIT.md` for the reasoning per day. No external research was opened for these
three days. This is a deliberate outcome of "research only what is needed," not an oversight: three
of four days in this batch were judged already strong enough that spending research budget on them
would have been research for its own sake, which the task instructions explicitly rule out.
