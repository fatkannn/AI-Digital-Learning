# M17.8 (Batch 5) — Research Sources: Days 17–20

**Zero external sources were opened this batch.** All four days (17–20) were classified KEEP
after direct pedagogical audit of the existing lesson content — no external claim needed
verification because no time-sensitive, independently-checkable factual claim was present that
the existing text didn't already handle correctly and durably.

This is stated plainly, not padded with sources that weren't actually needed, consistent with
the instruction to research only what is necessary and to stop once a day can be honestly
classified KEEP.

## Candidate angles considered and resolved without external research

Two days warranted a deliberate "should I actually go check this" pause before landing on KEEP,
both documented in full in `M17.8-DAYS-17-20-AUDIT.md`:

1. **Day 17 (Prompting / Structured Output).** Considered whether the structured-output
   guidance ("parse defensively, validate against schema, define failure behavior") might be
   stale given how fast that specific API surface moves across vendors. Resolved by direct
   inspection: the lesson deliberately teaches the durable, vendor-agnostic principle rather
   than any single vendor's current guarantees, and vendor-specific API mechanics are explicitly
   the territory of other days (7 and 22). No external source needed because no vendor-specific
   claim is actually being made in the lesson text.

2. **Day 20 (Tool Calling and AI Agents).** This is one of the fastest-moving topics in the
   curriculum, so a currency check was treated as warranted rather than assumed unnecessary.
   Resolved by direct inspection: the five safety controls taught (least privilege, human
   approval, iteration/cost limits, logging, treat-content-as-data) are architecture-level
   principles, not tied to any specific product feature, metric, or named phenomenon that could
   independently go stale — unlike Day 16's "context rot" in the prior batch (M17.7), which was
   a specific, named, sourceable claim worth opening a real source for. No equivalent single
   checkable claim exists on Day 20 to research.

## Comparison to prior batches, for calibration

- M17.1 (Days 1–4): 2 sources opened (both A), 1 finding (Day 1 NPU).
- M17.4 (Days 5–8): 2 sources opened (both A), 0 content findings (2 assessment-only flags).
- M17.6 (Days 9–12): 0 sources opened, 0 findings.
- M17.7 (Days 13–16): 1 source opened (A), 1 finding (Day 16 "context rot") — implemented.
- **M17.8 (Days 17–20, this batch): 0 sources opened, 0 findings.**

The zero-source outcome here is consistent with M17.6's — not every batch produces a finding,
and forcing external research to "justify" a research effort would violate the explicit
instruction not to over-research once a day can be honestly classified KEEP.
