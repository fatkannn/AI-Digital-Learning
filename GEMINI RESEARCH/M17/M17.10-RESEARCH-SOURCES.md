# M17.10 (Batch 7, FINAL) — Research Sources: Days 25–28

One external source actually opened this batch, for Day 28, credibility **A**. Days 25, 26, and
27 required none — 25 and 27's content is stable/tool-agnostic and already well-sourced; 26 and
27's resources were both directly re-verified with full content checks three batches ago in this
same project (M15 Batch 3/§41), fresher than anything a new fetch this session could add.

---

## Source 1

- **Title:** "Specification" (Model Context Protocol, 2025-06-18 revision)
- **Organization:** Model Context Protocol (open-source project)
- **Author:** Not individually attributed (community/project specification document)
- **URL:** `https://modelcontextprotocol.io/specification/2025-06-18` — the same URL already
  cited as Day 28's own first resource, opened again directly (not a new/different citation)
- **Source type:** Official primary source — the authoritative specification itself
- **Credibility level:** **A**
- **Affected day:** Day 28
- **Relevant finding:** **FACT**, confirmed by direct line-by-line comparison against the
  lesson's own text: the three roles (host/client/server) match exactly; the three server
  capabilities (resources/prompts/tools) match exactly, including their descriptions; JSON-RPC as
  base protocol confirmed; client-offered sampling and elicitation confirmed (the spec also names
  a third client capability, "roots," which the lesson does not mention — an omission within the
  lesson's own stated scope, not an error); the security section's specific claims (arbitrary
  code execution, untrusted tool descriptions unless from a trusted server, mandatory explicit
  consent before invoking any tool) all match the spec's actual wording closely enough to confirm
  accurate paraphrase, not invention or softening.
- **Exact reason it matters:** MCP is the fastest-moving topic in this 28-day curriculum (the
  lesson's own resource already discloses a newer spec revision exists), which made checking
  whether the *lesson's own teaching* — not just its citation — still matches the current
  mechanics the single highest-value research target in this batch.

---

## Recommendation status

**No recommendation.** This check produced a confirmation of existing accuracy, not a finding
requiring action. Recorded in full per this batch's own discipline (established in M17.9/§52):
a fast-moving topic having been checked and found correct is worth documenting on its own terms,
not only logged when something needs to change.
