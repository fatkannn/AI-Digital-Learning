# M17.4 — Research Sources: Days 5–8

Every external source actually opened this session via WebFetch. No search-result snippet was
treated as evidence on its own without opening the underlying page. No URL was guessed or
constructed — every URL opened is a resource URL already stored in the current curriculum.

| # | Source | Organization | URL | Type | Relevant finding | Affected day | Credibility |
|---|---|---|---|---|---|---|---|
| 1 | "Learn web development" | MDN (Mozilla) | `https://developer.mozilla.org/en-US/docs/Learn` | Official vendor/foundation documentation hub | **FACT:** the URL loads MDN's general web-development learning hub (title "Learn web development \| MDN", last updated August 2025) — broad in scope, not specific to client-server architecture. **INFERENCE, not confirmed:** whether the exact stored path is a legacy alias for MDN's current path (`/docs/Learn_web_development`) — raw HTTP redirect headers were not available to confirm this either way; only that real, current, on-topic content loads when the URL is opened | Day 5 (primary finding), Day 6 (same URL, same finding, not re-fetched) | **A — Official primary source**, with the specificity caveat noted above treated as a **RECOMMENDATION** for future attention, not a fact about the resource being broken |
| 2 | "HTTP: Hypertext Transfer Protocol" | MDN (Mozilla) | `https://developer.mozilla.org/en-US/docs/Web/HTTP` | Official vendor/foundation documentation | **FACT:** resolves directly, no redirect; comprehensively covers request methods, the full current header set (170+, including CORS/CSP), authentication, and the complete status-code reference (1xx–5xx) — fully supports the day's claims about the resource with no currency concerns found | Day 7 | **A — Official primary source** |

## Sources considered unnecessary

Day 8 required no external research: the pedagogical audit found the lesson content strong and
appropriately scoped, and the one finding (document databases untested in the quiz) is a
structural observation about the existing content, not a claim requiring external verification.
PostgreSQL's official tutorial and freeCodeCamp's SQL path (Day 8's two resources) were read
against the lesson's own claims and judged consistent without needing a fresh fetch — no currency-
or accuracy-sensitive claim was at stake for either.

Postman Learning Center (Day 7's second resource) was not re-fetched this session; it was already
back-verified to its canonical path in a prior session (M9/M10, per the resource's own `hubungan`
field) and no new claim about it was being made this session.

## Note on research proportionality

Two sources opened across four days, both for resource-currency questions rather than lesson-
content gaps — a lighter research load than M17.1 (also two sources, but for a genuine content
enhancement). This batch's honest finding is that Days 5–8 needed less outside verification than
Days 1–4 did, not that research was skipped. Manufacturing additional fetches to look more
thorough would have violated the "research only what is needed" instruction.
